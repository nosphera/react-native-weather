import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    width: '100%',
  },
  map: {
    height: '100%',
    width: '100%',
  },
});
export default styles as any;
